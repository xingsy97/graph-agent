# Graph Agent presentation

The deck is built with [Slidev](https://sli.dev/) and deployed to GitHub Pages.

```bash
pnpm install
pnpm deck:dev
```

Build the static deck with:

```bash
pnpm deck:build
```

Speaker notes are stored directly in `slides.md`. Press `P` in Slidev to open presenter mode.
