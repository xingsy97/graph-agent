# Copilot is the default. Set mock explicitly for development without authentication.
AGENT_RUNTIME=copilot
# Optional. If unavailable, Graph Agent selects a supported model automatically.
COPILOT_MODEL=auto
# GITHUB_TOKEN=
